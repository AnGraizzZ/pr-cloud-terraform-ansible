users:
  - name: agz
    groups: sudo, docker
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    ssh_authorized_keys:
      - ${ssh_public_key}

chpasswd:
  expire: true       # Требует смены пароля при первом входе
  list:
    - agz:gjkmpjdfntkz